{
  "name": "da-msm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:stage": "wrangler deploy --env stage"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
